# add local bin to PATH
export PATH="$HOME/.local/bin:$PATH"

if command -v mise &> /dev/null; then
    eval "$(mise activate bash)"
fi

_1p_sock="/run/user/$(id -u)/ssh-agent-1p.sock"
if [[ -S "$_1p_sock" ]]; then
    export SSH_AUTH_SOCK="$_1p_sock"
fi

